view:

caso seja secretária:
SELECT * FROM Horarios_Agendados_Veterinario;

caso seja vet = id_vet:

SELECT * FROM Horarios_Agendados_Veterinario WHERE id_veterinario = $s;


procedure:

CALL realizar_agendamento(id_animal, id_veterinario, data, hora);


inserindo a foto:
UPDATE Usuario 
SET foto = pg_read_binary_file('/caminho/da/imagem.jpg') 
WHERE id_usuario = 1;


recuperando a foto:
SELECT foto FROM Usuario WHERE id_usuario = %s;
